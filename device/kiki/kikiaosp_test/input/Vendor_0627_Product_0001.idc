# QEMU's virtio keyboard uses a virtio bus, so Android does not infer that
# the host keyboard is an external peripheral from the bus type alone.
device.internal = 0
keyboard.layout = Generic
keyboard.characterMap = Generic
